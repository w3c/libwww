## Process this file with Automake to create Makefile.in.

DEF := $(wildcard *.def)
MAK := $(wildcard *.mak)
WIN := $(wildcard *.rc *.ico *.h *.c)

DIST_OTHER = \
	$(DEF) \
	$(MAK) \
	$(WIN)

all-local : $(DIST_OTHER)

